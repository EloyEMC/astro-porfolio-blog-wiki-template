---
import { formatDate } from '../utils/date';

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="legacy-post-date-value">
  {formatDate(date)}
</time>
