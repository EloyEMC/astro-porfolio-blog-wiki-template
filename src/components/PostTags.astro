---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="legacy-post-tags">
  {tags.map((tag) => (
    <a href={`/tags/${tag}`} class="legacy-post-tag">
      #{tag}
    </a>
  ))}
</div>
